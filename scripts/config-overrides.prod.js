function reactDevConfig(config,env){
    //Include all the configurations for the development environment
    console.log('development config '+env);
    return config;
};

module.exports = reactDevConfig;
